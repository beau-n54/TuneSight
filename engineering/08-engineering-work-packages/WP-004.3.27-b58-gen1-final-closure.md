# WP-004.3.27 — B58 Gen1 Final Closure

Status: both remaining relationships reached deterministic hard evidence boundaries; neither was admitted.

Authority boundary: this investigation does not select a conflicting representation, fabricate binary membership, publish applicability, or grant EDIT, EXPORT, FLASH, Stock, or Reference authority.

## Task 1 — `000030765A3C06`

The exact conflict is a three-way storage overlap at binary address `7,340,032`:

- `GPF - Rail Pressure Setpoint Reconditioning` (`BMWgpfheat_p_RailTstrPcg_C`) is a valid unsigned, big-endian 16-bit scalar using `X*0.000610351563`.
- `GPF - Rail pressure setpoint for multiple injection, 1/1` (`BMWgpfheat_p_RailInjMpl_M`) declares a 6×6 unsigned, big-endian 16-bit map. Its x, y, and z representations all begin at the same address. Its value equation is `X*0.000610351563`.
- `GPF - Rail pressure setpoint for single injection, 1/1` (`BMWgpfheat_p_RailInjSng_M`) declares the same storage and equations but a different calibration-function label. Its units are absent while the multiple-injection occurrence declares `RPMx1000`, `%`, and `MPa`.

Both exact repository binaries contain identical bytes across the overlap and begin with `c3 c3`; the valid scalar therefore decodes raw `50,115`. Identical bytes do not resolve the conflicting semantic claims. The two maps occupy the same 72-byte value footprint and overlap the scalar at its first two bytes. Neither map is marked Legacy, duplicate, or a governed alias. Selecting either would silently discard a different source-declared calibration function. Quarantine cannot prove dependency isolation because the target storage overlaps another valid Definition.

Result: `BLOCKED_TECHNICAL_CONFLICT`. The relationship remains fail-closed.

## Task 2 — `000030765A5005`

The XDF repeats the same three Definition revisions, address, representations, equations, distinct calibration-function labels, and unsafe scalar/map storage overlap. The alternate source bindings do not turn the occurrences into governed aliases.

The repository-wide responsible-binary search examined all 217 controlled BIN artifacts. Neither ASCII nor exact hex-encoded `000030765A5005` occurs in any artifact. Existing validation records consistently report zero responsible binaries, and no accepted physical artifact, historical mapping, or marker profile establishes an exact substitute. Filename, family, adjacent software identity, shared Definition revision, and similar layout are not membership authority.

Result: `BLOCKED_MISSING_BINARY_EVIDENCE`, independently accompanied by `BLOCKED_TECHNICAL_CONFLICT`. Admission requires an exact controlled Engineering Binary with a deterministic digest, supported container resolution, exact marker observations, compatible byte length/address space, complete extraction/conversion validation, and separate resolution of the overlapping representation claims.

## Final B58 Gen1 state

- XDF sources: 7
- active Current VIEW relationships: 5
- quarantine-aware VIEW relationships: 4
- blocked: `000030765A3C06`, `000030765A5005`

The accepted Working Calibration and generic EDIT architecture are unchanged. EXPORT and FLASH remain separately unqualified.
