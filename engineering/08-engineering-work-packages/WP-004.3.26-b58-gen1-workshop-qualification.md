# WP-004.3.26 — B58 Gen1 Workshop Qualification

Status: exact cohort blockers refined; one Source Authority decision candidate materialized; no Source Authority, applicability, publication, or B58 Workshop admission accepted.

Authority boundary: Founder Founder authorized investigation and preparation of exact governed-Evidence artifacts. This package does not infer that a physical upload equals a repository artifact, accept the pending candidate, publish Definition coverage, qualify Stock, select a conflicted representation, or introduce editing or DME write authority.

## Founder physical evidence and reproducibility boundary

Founder physically observed a real F30 B58 Gen1 stock upload resolve to `ROM_RECOGNIZED_DEFINITIONS_UNAVAILABLE`, and a real modified B58 Gen1 upload of approximately 8,388,608 bytes resolve to `INVALID` with no established ROM/software identity. Both failed closed without Development Preview fallback.

The physical files, exact SHA-256 digests, filenames, and provider result payloads are not repository-controlled. Consequently neither upload can responsibly be declared equal to any master-library binary and their exact marker offsets, multiplicity, container subtype, ECU/DME observation, or Definition relationship cannot be reconstructed here. The next evidence capture must retain the provider-reported digest and byte length or supply the exact artifact for read-only hashing. Person, vehicle, approximate size, and filename are not identity authority.

The provider scans the complete resolved Engineering Binary for every governed B58 Gen1 identity in exact ASCII and seven-byte hex-encoded form. A deterministic 8 MiB control with no marker remains `INVALID`; the same control with a governed hex marker at the end resolves that exact ROM and remains `ROM_RECOGNIZED_DEFINITIONS_UNAVAILABLE`. Thus size and marker offset do not explain the modified physical result. With its bytes absent, the remaining responsible finding is `PHYSICAL_ARTIFACT_REQUIRED`: it may be a distinct ROM, a full-flash/virtual-read/transformed artifact, or an artifact whose identity material is absent or represented outside the currently evidenced encodings. Adding another encoding without observing its bytes would be guessing.

## Exact repository cohort

Every available binary is a raw BIN. Whole-binary cross-cohort scanning finds only its own exact seven-byte hex marker, normally three times at offsets 262469, 6814977, and 7863823. This disproves the earlier blanket identity-ambiguity classification for the six relationships with binaries; it does not create Source Authority or applicability.

| Exact ROM/software | Repository binary evidence | Definition/conversion state | Governed next state |
| --- | --- | --- | --- |
| `00003076501103` | MapSwitch `2cfb53c8…` / 7,864,320; Original `4f8c12a0…` / 7,864,576 | complete extraction; MapSwitch `Injector Scalar (Auto)` invalid | conversion/data exception plus Source Authority |
| `00003076501D02` | MapSwitch `33f6e7c4…` / 7,864,320; Original `010680ba…` / 7,864,576 | complete extraction; same MapSwitch invalid | conversion/data exception plus Source Authority |
| `000030765A3C06` | MapSwitch `8b3cb4c9…` and Original `11819006…`, both 7,864,320 | two representation-conflicted Tables; same MapSwitch invalid | representation and conversion/data decisions plus Source Authority |
| `000030765A5005` | no responsible binary | two representation-conflicted Tables; no extraction evidence | exact binary and representation decision |
| `00003081501102` | MapSwitch `c59341d9…` / 7,864,320; Original `7280bc64…` / 7,864,576 | complete extraction; same MapSwitch invalid | conversion/data exception plus Source Authority |
| `00003081501D04` | MapSwitch `ba22ac9a…` / 7,864,320; Original `eeb22414…` / 7,864,576 | complete extraction; same MapSwitch invalid | conversion/data exception plus Source Authority |
| `00007972000705` | Original `fb3402ee…` / 7,864,320 | 1,096/1,096 extraction; 811 converted; 285 identity; zero invalid, malformed, unavailable, or conflict; 120 unresolved units | exact Source Authority decision candidate |

The abbreviated digests above are presentation labels; executable evidence and the decision candidate bind full digests and immutable revisions.

## Common invalid-numeric root cause

All five MapSwitch failures are the same exact Definition revision `xdf-definition-revision:42026e596f3a1b7220bf5c609596e38b5cdb0e8138fd21ac9576e3023cb8ad35`, `Injector Scalar (Auto)`, address/raw offset 7,445,148, units `cc/min`. The raw value is zero and equation `1/(X*0.000011518903) * 80.11750567498999` therefore produces division by zero. Each Original binary succeeds. This is genuine input/equation interaction, not a parser or arithmetic implementation defect; zero is not sanitized. An exact Table-unavailable exception, corrected source revision, or evidence-backed MapSwitch data decision is required.

## Representation conflicts

`000030765A3C06` and `000030765A5005` each contain the same two conflicted occurrences at address 7,340,032:

- multiple-injection revision `xdf-definition-revision:cdb3a13ebbc80129453d0899731c77a72efcf33580e2ae1c8abb520e05440938` carries `RPMx1000`/`%`/`MPa` with `X`, `X*0.048828125`, and `X*0.000610351563`;
- single-injection revision `xdf-definition-revision:f470b44e75278b2d7d7e22850fe53746cf09b7f67c289c359c69d80b420c11bb` uses the same storage and scale equations with units absent.

Repository evidence does not establish an authoritative semantic winner. The affected Tables are intrinsically blocked. Existing conservative admission policy still blocks each exact relationship until an explicit Table-level exception or corrected representation is governed; this is a policy gate, not proof that unrelated Tables are structurally invalid.

## Exact Source Authority decision candidate

Only `00007972000705` is technically clean enough to advance in this task. The deterministic candidate is:

- candidate: `xdf-source-authority-candidate:ce47e482fd14b0cc48ba5a62518ca64d7daded637c30ae144464d14a2a2f7099`;
- revision: `xdf-source-authority-candidate-revision:1b3913abf5688ded6e82e90d592dcad2e3ebd9fe9fbed3e57f4c6b1b3456ac97`;
- source artifact: `xdf-source:a2113f0eb25fe42c378eb1b47de7032990e2b8b59eeb58a42ae655eb5c375a60` / `sha256:a2113f0eb25fe42c378eb1b47de7032990e2b8b59eeb58a42ae655eb5c375a60`;
- Definition Set: `xdf-definition-set:094b147cb350c17584202cedc8a45d11619ddbedff3151e44422497b28042be9` / `xdf-definition-set-revision:e0481c51a69b45aa9328dba345ab75cd997b459f58ca150a79348e3a36b03105`;
- binary: `fb3402eeac93ea1ae08cffecb31960923019444ecb22b057fd60b352dc51dd40`, 7,864,320-byte BIN;
- validation revision: `bmw-full-validation-revision:b960332ee5ae6d9ab39837b037bf1abe82221c4f1b998a628eee6341b44fb0f3`;
- state: `pending_founder_authority`.

Limitations remain binding: upstream acquisition and XDF authorship are unknown, Stock authenticity is candidate-only, no comparison mate is repository-controlled, and applicability acceptance/publication require later exact decisions. Founder Source Authority acceptance is the next decision; it would still not itself publish Workshop coverage.

## Current-only Workshop and admission state

The Workshop View Model currently requires qualified Reference and Current Datasets plus comparison evidence. A safe generic Current-only mode would make Reference values, deltas, changed/unchanged/axis-changed counts, and all comparison-derived presentation explicitly unavailable while retaining Table list, Grid, structurally applicable 2D/3D, Cell Inspector, and structural information. No B58 relationship has yet crossed Source Authority and exact applicability, so implementing and validating that pervasive nullable contract now would not admit a physical B58 upload and would outrun the decision gate. It remains the bounded platform-neutral implementation immediately after an exact single-binary relationship is accepted; the Current must never be reused as Reference.

No B58 Gen1 relationship is presently Workshop-ready. The subscriber provider correctly recognizes repository binaries but returns `ROM_RECOGNIZED_DEFINITIONS_UNAVAILABLE` because its published exact-coverage registry contains only accepted N54 relationships. No B58-specific UI branch or Development Preview fallback is introduced. Exact relationships can advance independently; family-wide completion is not required.

## Required next evidence and decisions

1. Founder supplies the provider digest/result details or exact Founder stock artifact. TuneSight then compares exact digest and marker observations with the cohort and identifies its precise relationship without filename inference.
2. Founder supplies the exact third-party artifact for read-only container, digest, encoding, entropy/layout, and whole-binary marker analysis. If a legitimate generic representation is evidenced, extend the governed extractor and regression suite.
3. Founder accepts or rejects Source Authority candidate `xdf-source-authority-candidate:ce47e482fd14b0cc48ba5a62518ca64d7daded637c30ae144464d14a2a2f7099` with its limitations.
4. If accepted, construct the separate exact applicability candidate and obtain the required Founder applicability/publication decision before adding the relationship to subscriber coverage. Implement generic Current-only presentation if no responsible Reference exists.
